import * as types from '../constants/story_action_types'

const initialStoryState = {

}

function story( state = initialStoryState, action){
  switch(action.type){

    case '':
      return state

    default:
      return state
  }
}

export default story
