import * as types from '../constants/home_action_types'

const initialHomeState = {

}

function home( state = initialHomeState, action){
  switch(action.type){

    case '':
      return state

    default:
      return state
  }
}

export default home
